<div class="d-flex justify-content-center mt-5">
  <div class="text-center mb-5">
    <%= form_with model: [book, Rating.new], local: true do |f| %>
      <div class="rating">
        <% (1..5).reverse_each do |stars| %>
          <%= f.radio_button :stars, stars, id: "rating_#{stars}" , checked: (book.ratings.find_by(user: current_user)&.stars == stars) %>
          <%= f.label :stars, '☆', for: "rating_#{stars}" %>
        <% end %>
      </div>
      <div class="buttons mt-0">
        <%= f.submit "Submit", class: "btn btn-info px-4 py-1" %>
      </div>
    <% end %>
  </div>
</div>